# pnpm configuration
# Allow build scripts for specific packages with native builds
# This is controlled by package.json's pnpm.onlyBuiltDependencies field
# These settings only apply to pnpm, not npm (which may show warnings when using npx)

enable-pre-post-scripts=true
